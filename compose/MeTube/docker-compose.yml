version: "3"
services:
  metube:
    image: alexta69/metube
    container_name: metube
    restart: unless-stopped
    ports:
      - "6789:8081"
    volumes:
      - /export/shared_folder/Music:/downloads
